import java.util.*;

class Question_16
{	
	/*
		16.Complete the following main() function.
		p s v main()
		{
			double num[];
			num = scanArray();
			printArray(num);
		};
		scanArray() function scans array of 6 double values.printArray() function prints this array.

	*/
	public static void main(String [] xyz)
	
	{
		double num[];
		num = Question_16.scanArray();
		Question_16.printArray(num);
		
	}
	
	static double[] scanArray()
	{
		double [] num = new double [6];
		Scanner sc = new Scanner (System.in);
		for(int i = 0;i<num.length;i++)
		{
			num[i]=sc.nextDouble();
		}
		return num;
	}
	
	static void printArray(double [] n)
	{
		for( double x : n)
		{
			System.out.println(x);
					
		}
	}

}
